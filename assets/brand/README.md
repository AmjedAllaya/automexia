# Brand assets

The checked-in SVG is an explicitly non-release placeholder created for local
builds and CI metadata validation. It is not derived from Rio artwork.

Stable publication remains blocked until Automexia supplies and approves an
editable SVG logo and app mark, monochrome/light/dark variants, a 1024×1024 PNG,
multi-size Windows ICO, standard macOS ICNS, Linux SVG and 16–512 px PNG exports,
and written redistribution-rights evidence. Update `ASSET-MANIFEST.toml` only
after every checksum and rights record is reviewed.
