Internal

- Add bounded semantic table data and host admission contracts with stable
  identities, exact typed values, strict decoding, lifecycle and permission
  regression tests. These contracts do not activate a new browser UI.
- Add table decode benchmarks, allocation cleanup checks and a fuzz target.
- Keep table diagnostics bounded and identifiers redacted without changing
  authorized display data; local UI frames do not authorize public distribution.
