Changed

- Bound the summarized all-feature workspace test to 30 minutes and 16 MiB of
  captured stdout, with complete Unix process-group or Windows Job Object
  termination on timeout and overflow.
- Added real child-process tests plus policy mutations for success, deadline,
  output overflow, bounded diagnostic retention, zero bounds, cleanup ownership
  and contract relaxation.
- Recorded the intermittent protected-push stall without attributing it to a
  product test that did not reproduce in sequential or repeated parallel runs.
