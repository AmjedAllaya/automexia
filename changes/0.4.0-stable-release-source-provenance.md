# Stable-release source provenance and repository assurance

- Added a digest-pinned stable-release policy and bounded checker for exact
  annotated release/fork tags, remote `main`, complete clean history, linearity,
  and author-matching DCO provenance.
- Made stable tag preflight require a protected read-only repository audit; any
  hosted failure or external account prerequisite blocks publication.
- Added real temporary-Git mutation coverage and registered it in repository
  validation, per-feature reinforcement, and full QA.
- Put all stable-release, signing, S1/S2, assurance-policy, and xtask release
  owners behind the exact-head two-review protected-path policy.
- Published the audited annotated Rio fork-point provenance tag while leaving
  the historical DCO exception and all legal, credential, reviewer, account,
  native-host, and elapsed-baseline prerequisites explicitly unresolved.
