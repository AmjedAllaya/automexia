Security

- Added revisioned compare-and-swap persistence for OpenSSH favorites, tags, and
  recent-use metadata with a private no-follow writer lock and overflow/stale-
  writer rejection.
- Retained one validated previous generation, reported fallback origin
  truthfully, and required exact reviewed revision for explicit recovery.
- Added contention, malformed-primary, recovery, legacy migration, atomic
  rotation, no-follow, bounded serialization, and Windows private-ACL coverage
  without adding process, network, credential, or PTY authority.
