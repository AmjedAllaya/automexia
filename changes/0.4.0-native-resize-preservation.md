# Fixed

- Preserve retained output and prompt spacing across ConPTY width/height changes,
  including wrapped lines crossing the scrollback/live viewport boundary.
- Prevent historical native repaint rows from becoming active prompt content;
  preserve the separate Unix reflow and explicit editor-erase contracts.
- Commit ConPTY and grid dimensions through the same worker resize transaction,
  preserving output during coalesced drag bursts. Keep bounded search from
  crossing non-content wrapped-line padding endpoints.
- Propagate native resize errors through the existing retryable adapter instead
  of panicking. Add live-shell, byte-fragmentation, selection/search, Unicode,
  snapshot, cleanup and resize benchmark coverage. Native desktop and unexecuted
  platform validation remain separately reported.
