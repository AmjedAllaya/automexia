### Fixed

- Preserve least-privilege Linux publication when GitHub omits REST ruleset
  bypass details: independently verify bounded GraphQL evidence and reject
  missing, redacted, inconsistent, nonempty or incomplete protection data.
- Add real CLI and workflow-mutation regression coverage and clarify the
  distinct whole-file Base64 Minisign and raw PEM GitHub App secret formats.
