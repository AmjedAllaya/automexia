### Fixed

- Isolated the history-audit routing mutation from the process-wide assurance
  cache lease so the complete zero-cost release profile can execute its own
  mutation suite without a recursive Windows file-lock failure. Real Gitleaks
  execution remains covered by the separately pinned scanner canaries.
