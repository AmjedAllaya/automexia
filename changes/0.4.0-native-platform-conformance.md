Fixed

- Move PowerShell command-start metadata to the documented line-reader
  boundary, preserve user history policy, and clear obsolete PSReadLine repaint
  cells before command output so resize storms cannot corrupt short listings.
- Run locked all-feature Rust and Unix shell integration contracts on every
  applicable native Windows, Linux, and macOS pull-request host.
- Make the Unix shell-source validator compatible with BSD/macOS `mktemp`.
