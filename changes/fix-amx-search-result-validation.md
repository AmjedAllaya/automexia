# Validate complete local search results

- Prevent `amx find text` from displaying earlier matches from files that ripgrep
  subsequently reports as binary; reject incomplete or ambiguous result streams.
- Preserve bounded output and result order without another file scan or process.
- Add permanent native late-binary, exact search-example, Git fetch/rewrite,
  editor identity/preference and offline-client failure regressions.
- Strengthen complete-output benchmarks, publication-order mutation checks and
  documentation of client deadlines and external desktop validation.
