# Fixed

- Adapt selected post-fork terminal-engine correctness and performance fixes
  for graphics protocol replies and memory accounting, background colors,
  combining-character damage, bulk control-string parsing, synchronized
  updates, viewport reflow, legacy emoji selectors, and safe modifier-click
  hints.
- Add focused parity, lifecycle, memory, interaction, and resize-storm
  regressions for every adapted behavior, including virtual-image deletion,
  background transparency, and render-snapshot convergence, while retaining
  Automexia's semantic prompt, custom chrome, pane, packaging, and identity
  contracts.
- Apply PowerShell filesystem formatting and PSReadLine colors before the first
  editable prompt, preventing asynchronous callbacks from stalling the first
  command, Up Arrow history recall, or Ctrl+R history search under ConPTY.
