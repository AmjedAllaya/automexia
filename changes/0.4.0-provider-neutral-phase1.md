Changed

- Extract the private provider-neutral extension API/runtime, local DevOps, and
  renderer-independent UI-model crates while retaining one application-owned
  PTY/session launch path.
- Render local operational context through bounded versioned generic status
  contributions with centralized responsive, accessible, grapheme-safe, and
  semantic-color policy.

Fixed

- Prevent deserialization from bypassing extension contract limits, prevent a
  fast provider worker from publishing before operation registration, cancel
  stale capsule work, and isolate cache invalidation to the rebound session.
- Remove unused `cdylib` outputs from the private Rio backend and Sugarloaf
  libraries so release benchmark builds cannot race on un-hashed DLL names.

Security

- Keep environment values and resolved secrets outside serialized launch and
  capsule contracts, redact secret-adjacent debug output, reject unknown or
  unsupported schema data, and add no new network/process/clipboard authority.
