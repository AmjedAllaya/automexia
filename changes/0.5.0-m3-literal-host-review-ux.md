Fixed

- Simplified the transient SSH host editor to one visible Cancel path while it
  owns the modal, preventing the redundant top-level close hit target from
  overlapping the field on small scaled viewports.
- Added direct screen-layer coverage for the case-insensitive `L` mnemonic and
  verified that Control, Alt, Super, and multi-character keys are not stolen.
- Re-audited F5.1/M3 and kept the phase **Partially done**: non-activated
  preparation/review is locally complete, while protected M2 activation, real
  OpenSSH/PTY lifecycle, and controlled native evidence remain required.
