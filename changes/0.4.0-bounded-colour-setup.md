### Performance and reliability

- Remove repeated regular-expression compilation and temporary buffers from
  terminal palette conversion. Default colours and borrowed colour helpers
  allocate no heap buffers and preserve exact colour values.
- Reject malformed Unicode colour digits without panicking; retain bounded
  input inspection, numerical/serde regressions and setup benchmarks.
