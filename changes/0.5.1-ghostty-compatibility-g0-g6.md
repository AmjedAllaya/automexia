# Added

- Add explicit pinned `ghostty-1.3` and moving `ghostty` keybinding profiles while
  preserving `automexia` as the implicit default, with bounded typed bindings,
  exact unbind/fallthrough/sequence/table/chain behavior, and atomic reload.
- Add generated action/keybinding inspection, dry-run-first Ghostty migration,
  finite fractional font sizing, exact absolute/line/fractional scroll
  translation, registry-derived palette hints, and secure bounded screen export.
- Add a modal redacted compatibility inspector with active/parked counts,
  bounded newest-first summaries, pointer/keyboard newest restore, two-step
  clear, and guaranteed no-PTY key isolation, plus memory-only parked-PTY undo/
  redo for complete closed top-level tabs.
- Add host-independent fixture verification, nightly compatibility fuzz and
  benchmark coverage, a mutation-tested repository policy, and a strict private
  exact-commit Windows/Linux/macOS evidence validator with redacted QA output.
  Native macOS/Linux release evidence, controlled three-platform lifecycle/
  visual/accessibility/package proof, the activated 30-day baseline, and
  individual split/local-tab/native-window history remain explicitly gated.
