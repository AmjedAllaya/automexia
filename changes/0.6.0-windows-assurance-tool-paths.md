# Fixed

- Compile pinned Cargo assurance tools and unpack their dependency sources in a
  disposable same-drive short path on Windows, atomically publish only bounded
  executables to the repository-local cache, and remove the build tree so deep
  worktrees no longer lose pre-push security checks to MSVC path ceilings.
- Run Cargo Audit, Cargo Deny, and Cargo Vet with a disposable same-drive short
  Cargo home on Windows so nested advisory Git paths cannot exhaust Win32 path
  space after the tools have been installed.
