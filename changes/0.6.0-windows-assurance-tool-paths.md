# Fixed

- Compile pinned Cargo assurance tools and unpack their dependency sources in
  disposable staging owned by the single same-drive Automexia cache on Windows,
  atomically publish only a complete content-addressed toolset, and remove the
  build tree so deep worktrees no longer lose checks to MSVC path ceilings.
- Run Cargo Audit, Cargo Deny, and Cargo Vet with one bounded managed Cargo home
  so nested advisory Git paths stay short without creating caller-local or
  per-scanner cache roots.
- Remove every caller-local Git repository variable from temporary scanner
  canaries and assert the parent HEAD/worktree remain unchanged, preventing a
  pre-push canary from committing into the contributor's real branch.
