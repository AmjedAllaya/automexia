Security: complete the remaining local D3/F4 review and lifecycle boundary
without enabling managed SSH.

- Resolve and observe the exact broker-owned OpenSSH executable on a bounded,
  capacity-one application worker outside input, PTY, renderer, and startup hot
  paths.
- Publish generation-bound review results before route wakes, reject obsolete or
  mismatched preparations, enforce a 30-second freshness window, and require a
  second explicit decision before guarded launch.
- Preserve the hard-disabled activation constant and unverified linked package;
  protected exact-head approvals, loader attestation/revocation, and real native
  OpenSSH evidence remain fail-closed release prerequisites.
- Add bounded managed PTY-tree shutdown: Windows terminates the owned
  kill-on-close Job Object; Unix retains the waitable leader while signalling
  the owned process group, preventing force-cleanup from targeting a reused PID.
- Join PTY reader workers with a bounded deadline during Context and librio
  teardown.
- Ratchet the D0/D3 policy checker and mutations so review-worker ownership,
  publish-before-wake, current-review binding, managed tree shutdown, and
  platform cleanup markers cannot silently regress.
- Add deterministic current-review, worker-ordering, worker-join, and owned-tree
  lifecycle regressions and synchronize architecture, testing, readiness,
  feature, roadmap, and detailed implementation records.
