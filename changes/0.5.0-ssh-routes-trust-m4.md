Changed

- Added separate typed SSH host, optional user, and optional port review fields,
  plus bounded canonical config-defined ProxyJump chains with exact direct or
  routed OpenSSH argument vectors.
- Added complete first-use, known, and changed host-key algorithm/SHA-256 review,
  public agent/certificate/hardware identity evidence, and fresh-evidence launch
  binding. Changed keys remain blocked.
- Added mnemonic `C` as a user-owned copy handoff for the exact reviewed SSH
  command; it never executes, appends a newline, or injects Enter.
- Ratcheted the immutable session-launch contract to schema 4 while preserving
  byte-exact schema 1, 2, and 3 history.

Security

- Reject free-form options, ProxyCommand, remote commands, shell text, raw IPv6,
  ambiguous or excessive jump routes, duplicate public identity fingerprints,
  and any stale route/trust/status evidence. Omit path-like, control-bearing, or
  Unicode-format identity comments before they reach the review surface.
- Keep `known_hosts` mutation, key/agent/token custody, agent forwarding, public
  status execution, and production managed SSH disabled. The bounded status
  contract is exact `ssh-add -l -E sha256`, 2 seconds, 64 KiB, and 64 identities.

User experience

- Keep the Connection Hub compact with Host/User/Port focus order, route-aware
  summaries, full nontruncated Safety evidence, accessible copy semantics, and
  tested tiny-to-8K pointer/focus geometry.
