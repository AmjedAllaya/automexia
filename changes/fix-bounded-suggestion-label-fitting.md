# Bound suggestion label fitting and preserve highlight ownership

Use the shared measured fitter for plain and matched suggestion labels.
Generated ellipsis dots no longer inherit highlights from removed characters.
Reject nonfinite budgets without changing existing pixels; measure exactly
the styled runs drawn and borrow their source storage. Original command and
accessible values, overlay geometry and activation boundaries remain unchanged.
Regression coverage includes literal CPU frames, Unicode marker boundaries,
mixed styles, fractional scales and the real-font retained-source limit.
