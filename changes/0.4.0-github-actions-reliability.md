Fixed

- Repair hosted CI across private CodeQL, nightly fuzz/sanitizer/Miri jobs,
  current objc2 macOS builds, exact nFPM installation, and Windows ARM64 MSI
  packaging with a repository-pinned WiX 5 source.
- Add repository contracts that reject regressions to unsupported native Miri
  FFI, obsolete objc2 class macros, entitlement-dependent private SARIF uploads,
  missing sanitizer sources, invalid tool versions, or WiX 3 ARM64 packaging.
- Normalize canonical Windows paths before passing them to WiX so .NET does not
  reject Rust's verbatim path prefix while building the ARM64 MSI.
- Bound Miri to the pure scalar/parser and temporary-file transport regressions
  with a hard timeout, avoiding hour-long full-library runner stalls.
- Make Linux packages Lintian-ready with deterministic modes and compression,
  valid maintainer/description data, runtime dependencies, and required
  copyright/changelog documentation.