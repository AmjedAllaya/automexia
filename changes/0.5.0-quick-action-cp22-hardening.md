---
kind: fixed
area: command-productivity
---

- Keep shell-user and global-user Quick Action precedence distinct, revalidate
  every activation layer, and define optional empty placeholder behavior.
- Coalesce searches independently and fairly per pane with a hard 32-route
  ceiling, exact close cleanup, hostile-query denial, and joined shutdown.
- Reject secret-reference and exact-launch actions before collecting placeholder
  input, and render textual risk, source, conflict, loading, degraded-health,
  empty, and unavailable states in the Command Center.
