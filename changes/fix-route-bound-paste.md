Queue paste as one bounded transaction for its captured terminal. Secondary
clipboard clicks now target the pane under the pointer without clearing a
selection before copy/paste resolution. Reject stale destinations and text over
1 MiB with a content-free warning. Add exact-byte, ownership and boundary tests;
native graphical clipboard validation remains outstanding.
