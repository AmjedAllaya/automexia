Restore shell-owned Ctrl+R and Ctrl+D in current source defaults. Keep clone
actions discoverable and explicitly rebindable without rewriting user settings,
and show configured clone bindings instead of stale built-in labels.
