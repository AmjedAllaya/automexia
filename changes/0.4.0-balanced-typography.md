# Changed

- Rebalance the default terminal, prompt-tag, result, title, and local-tab type
  scale for denser multi-pane workspaces while preserving readable floors,
  accessible pointer targets, high-contrast semantic colors, and per-pane zoom.