Added

- Added pane-local `Shift`+Arrow terminal selection and Unicode-aware
  `Ctrl`+`Shift`+Left/Right word extension across Windows, Linux/BSD, macOS,
  PowerShell, CMD, WSL, Bash, and Zsh.
- Kept search, Vi mode, pinned image browsing, shell input, and explicit user
  bindings isolated, including balanced ConPTY key press/release handling.