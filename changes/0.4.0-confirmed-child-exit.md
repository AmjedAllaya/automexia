Fixed

- Preserve confirmed terminal child-exit notifications when queued input or final
  output transport fails. Keep cancellation priority, final output and a single
  exit/close publication sequence. Add deterministic worker ordering regressions
  and real PowerShell queued-input exit coverage with content-free diagnostics.
- Retain available final output across normal read batches, with a separate
  bounded, cancellable closing drain and exact native final-row checks.
