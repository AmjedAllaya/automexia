- Fixed contributor QA commands hanging after their parent exited while a child
  retained output. The runner now contains native process groups, preserves the
  command's exit and final output, and joins readers before releasing ownership.
- Added native process-identity, admission, timeout, capture and repeated-cleanup
  regressions. Unavailable native and hosted validation remains explicit.
