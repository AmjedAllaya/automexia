# Fixed

- Allow Windows PowerShell and PowerShell 7 profile hooks under OneDrive and
  other Cloud Files-backed Documents folders by recognizing Microsoft cloud
  reparse tags, while continuing to reject junctions, symbolic links, unknown
  reparse points, and substituted path ancestors during install, repair, and
  uninstall.
- Stream bounded canonical Base64 WSL install and cleanup programs through
  `wsl.exe` stdin instead of embedding them in the Windows command line, and
  discard PowerShell 5.1's UTF-8 preamble before decoding.
