Changed

- Add the test-only D3 exact-argument session-launch review boundary with fixed
  OpenSSH resolution, native executable identity, strict scope/argv/cwd/env
  policy, generation leases, revocation, and redacted audits.

Security

- Keep the entire managed-launch broker outside the production module graph
  until ADR 0012 is accepted; broad process spawning, shell evaluation,
  extension environment access, secret resolution, and unmanaged executables
  remain denied.
