# CI and release assurance hardening

- Run every Python CI checker/mutation suite in hosted policy and local full QA.
- Bind release coverage to exact internal release commits and the Windows/MSVC baseline.
- Harden coverage, Action pin, stable-release manifest, and vendored-license inputs with direct mutation tests.
- Report exact bounded uncovered changed-line identities and execute the CI contract inside the instrumented xtask test process.
- Keep shell lint and integration assurance scoped to tracked and newly added non-ignored repository sources.
- Correct contributor and release documentation without treating unavailable hosted/native evidence as passed.
