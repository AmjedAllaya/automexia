Security

Add a non-activated direct OpenSSH review contract for one concrete inventory
alias or bounded literal host. The pure model binds a single redacted argument
to the F2 profile/source/capsule revisions, canonical `ssh` identity,
`session.launch`, validated current identity observation, trust state, and fresh
review fingerprint; forged plans, inapplicable trust, misleading literal
targets, and hostile, ambiguous, or stale input fail closed. A responsive,
accessible Connection Review exposes only public summaries and remains disabled
while M2 protected approval, process/PTY ownership, and native evidence are
pending.
