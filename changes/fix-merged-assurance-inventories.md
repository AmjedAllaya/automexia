# Fix merged assurance inventories

- Reconcile the documentation coverage sentinel with the reviewed Automexia CLI command registry.
- Reconcile the feature-assurance benchmark sentinel with the merged text-compaction and semantic-table benchmarks.
- Restore the previously reviewed YAML and layout dependency generations rather than broadening supply-chain trust.
- Align corcovado's direct Windows types with miow's exact windows-sys generation so clean builds cannot mix nominally incompatible Win32 structures.
- Isolate the hostile-Zsh-completion regression from unrelated latency sampling so host contention cannot masquerade as a completion security failure.
- Retain explicit inventory assertions so future parser or discovery regressions fail closed.
