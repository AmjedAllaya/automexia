Added

- Add the disabled-by-default D4 OpenSSH inventory foundation with exact
  filesystem grants, bounded static aliases/includes, public-only metadata,
  private atomic storage, exact-file refresh orchestration, fuzzing, and
  cross-platform assurance without adding credential or network authority.
- Harden the completed boundary with immutable resource ceilings,
  race-resistant no-follow reads, bounded pre-staging JSON serialization,
  scanner-derived watcher plans, active obsolete-scan cancellation, and
  mandatory nightly/controlled benchmark ownership.
