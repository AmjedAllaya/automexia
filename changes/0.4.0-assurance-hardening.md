Security

- Bound normal config/theme loads and legacy migration trees, reject non-local
  image paths consistently, and add adversarial size, encoding, symlink,
  recursion, storage, and recovery tests.
- Add cross-platform worker/PTY lifecycle tests, PTY startup/throughput
  benchmarks, extension-runtime ASan/TSan coverage, and controlled execution of
  every Criterion target.
- Enforce a feature assurance ledger covering every workspace component, all
  benchmark and fuzz targets, eight quality dimensions, and native
  Windows/Linux/macOS workflow evidence without treating external hardware or
  untested Linux distributions as passed.
