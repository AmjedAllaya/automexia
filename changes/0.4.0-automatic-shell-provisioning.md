Changed

- Make `cargo dev` and `cargo automexia` automatically install or repair
  PowerShell, CMD, WSL, Bash, Zsh, and user-local terminfo support immediately
  before launch, with a source-aware idempotent fast path and fail-fast errors.
