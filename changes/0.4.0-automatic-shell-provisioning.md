Security

- Replace automatic launch-time profile/WSL provisioning with signed,
  session-only package resources. Persistent integration now requires the
  explicit `automexia shell-integration install` command, honors PowerShell
  execution policy, and transports WSL scripts as bounded raw UTF-8 to a fixed
  command instead of an encoded decoder pipeline.
