Changed

- Preserve schema 1 as hash-checked history and enforce the schema-2 D0/D3
  package, grant, audit, strict-default, executable-resolution, manual SSH,
  nine-boundary, and four-platform native fixture contract with mutations.
- Freeze hermetic loopback setup, isolated authentication state, bounded
  readiness/lifecycle, cancellation checkpoints, cleanup/resource invariants,
  evidence/artifact metadata, redaction surfaces, and WSL activation semantics.
- Bind the nonactivated review broker to exact package digest, version,
  contract compatibility, and reviewed/signed verification before resolution.

Security

- Keep managed launch outside production while ADR 0012 remains proposed;
  WSL, process, PTY, network, provider, authentication, key-custody, renderer,
  environment, secret, and shell authority remain disabled.
- Require the broker module declaration to remain directly test-gated and
  reject process spawn/wait/kill, network, and PTY primitives from the
  nonactivated review model.
