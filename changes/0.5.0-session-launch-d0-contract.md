Security: document the current public session-launch boundary:

- local sessions use a typed executable and exact argument array;
- one application-owned session controls PTY/process lifetime and cleanup;
- ordinary system OpenSSH retains credential, host-key, authentication, and network ownership; and
- unreleased managed connection details are maintained only in private documentation.
