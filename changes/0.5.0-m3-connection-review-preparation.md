Changed

Selecting a supported direct OpenSSH inventory record now opens a responsive,
accessible, disabled Connection Review. The application composes only one
current bounded record into a generation-bound canonical pending F2 plan,
requests only session.launch, keeps executable identity unresolved and every
runtime authority false, and invalidates stale selection/catalog/metadata/
runtime bindings. Compact Connection, Safety, and Launch groups keep readable
labels through the smallest supported viewport and add redundant icons,
semantic color, keyboard and pointer Back, while profile identifiers, exact
aliases, opaque references, digests, fingerprints, processes, PTYs,
credentials, and network activity remain outside debug, presentation, and
execution boundaries.