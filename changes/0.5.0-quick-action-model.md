# Added

- Add the capability-free CP2.0 typed Quick Action schema, bounded TOML parser,
  deterministic validator, validated-only wrapper, and versioned valid/hostile
  fixtures in `automexia-devops`.
- Enforce the exact three-file model boundary and reject filesystem, process,
  network, environment, UI, PTY, shell-profile, async-runtime, and unsafe-code
  authority before persistence or alias activation is introduced.
