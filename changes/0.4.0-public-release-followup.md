# Public Linux release follow-up

- Require cryptographically signed future commits on the passive public
  archive's protected default branch, independently of DCO sign-off.
- Prove the immutable-release workflow verifies every uploaded asset through
  the complete attestation loop before producing website activation evidence.
- Document the unrevised historical archive-identity exception accurately.
- Bound the credential-free Linux release-quality job for free hosted runners,
  and mutation-test concurrency, debug artifacts, cleanup order, and source-only
  cache ownership after a real fail-closed linker-resource regression.
- Bound each native Linux package job to one Cargo build job without release
  debug data, and mutation-test both limits after a hosted Arm64 runner shutdown
  interrupted an otherwise passing credential-free rehearsal.
