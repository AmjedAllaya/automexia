# Deterministic suggestion publication readiness

- Preserve the production 250 ms source deadline while isolating real
  suggestion-service integration cases from test-harness CPU contention.
- Build pure mailbox spoof, dismissal, supersession, saturation, and kill
  scenarios from deterministic ranked publications instead of unrelated worker
  scheduling.
- Record the all-feature readiness failure and require focused repeated evidence
  without treating retries as erasing the first result.
