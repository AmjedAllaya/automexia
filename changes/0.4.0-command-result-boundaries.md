# Changed

- Place completed-command exit state and duration on the following prompt's
  reserved context row and add a restrained semantic divider there, separating
  command output from the next editable prompt without adding terminal rows,
  output bytes, or another persistent UI surface.
