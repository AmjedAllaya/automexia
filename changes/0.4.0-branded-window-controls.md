### Changed

- Replaced the ungrouped custom minimize, maximize, and close glyphs with a
  compact Automexia liquid-glass capsule using cyan, purple, and coral accents.
- Added distinct rest, hover, held, active-window, and inactive-window states
  while preserving full right-edge caption hit targets.
- Made the maximize glyph switch to restore from the native window state.
- Changed custom caption actions to activate only when released over the same
  control, with drag-away, focus-loss, and live-configuration cancellation.

### Fixed

- Prevented a pressed custom caption action from leaking into terminal input.
