# Added

- Added selected-pane command navigation with `Ctrl+Shift+Up/Down` on
  Windows/Linux/BSD and `Cmd+Shift+Up/Down` on macOS. The same actions are
  discoverable in the command palette, use trusted shell-integration marks,
  preserve adjacent silent-command boundaries, stop cleanly at retained-history
  boundaries, and never write to the PTY.
