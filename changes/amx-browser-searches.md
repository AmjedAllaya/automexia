Added explicit `amx search` commands for Google, GitHub repositories, YouTube and
DuckDuckGo, plus official-documentation searches through `amx docs`. Existing
`amx google` behavior is preserved. All routes support offline URL preview,
bounded inputs, privacy-preserving errors and native-shell regression checks.
