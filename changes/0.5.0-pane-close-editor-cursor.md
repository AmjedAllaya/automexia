Fixed ConPTY prompt recovery changing the native cursor between erase/repaint
fragments. Windows pane/window resizing now uses a bounded asynchronous input
settle interval for PSReadLine compatibility; ordinary input and Unix PTYs are
unchanged. Buffered input is delivered before subsequent resize messages.
Added real ConsoleHost/PSReadLine split-close editing regressions, repeated
cleanup, cursor-ownership and deadline/ordering tests.
Fixed double-cleanup panics after native Windows pipe errors, with closed-peer
read/write regressions and shared-process editor lifecycle coverage.
