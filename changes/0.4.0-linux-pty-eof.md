### Fixed

- Normalized Linux PTY master-side `EIO` after a one-shot slave closes as the
  platform end-of-stream signal in the core PTY boundary, while preserving
  exact output, completion-marker, child-exit, and teardown checks.
- Kept temporary zero-byte ConPTY reads retryable and added native Linux and
  Windows lifecycle regression coverage, semantic checker mutations, repeated
  Linux race evidence, and a corrected throughput benchmark.
