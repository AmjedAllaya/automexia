## Added

- Added a private, versioned Connection Library store for bounded connection
  profiles, automation recipes, and preferences, including explicit reviewed
  recovery and redacted export/import with fresh identifiers.

## Security

- Added expected-revision compare-and-swap writes, same-directory atomic
  replacement, one validated previous generation, no-follow file handling,
  private-permission hardening, and deterministic read-only/disk-full failure
  tests that preserve the primary document.

## Documentation

- Reconciled the SSH/connectivity roadmap, phase audit, feature assurance, user
  boundary, and local Windows evidence without activating Connection Hub or
  managed SSH.
