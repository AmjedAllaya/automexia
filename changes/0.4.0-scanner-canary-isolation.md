# Fixed

- Isolate temporary Git-backed security scanner canaries from every caller-local
  repository variable, and prove the caller's HEAD, worktree status, and local
  configuration remain unchanged after the real canary is removed.
