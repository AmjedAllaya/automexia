### Fixed

- Aligned the public Linux manifest and installation guidance with the exact
  nFPM DEB revision used by the native x64 and Arm64 package jobs. The release
  gate now derives DEB and RPM filenames from the package configuration and
  retains the hosted rehearsal mismatch as a regression fixture.
