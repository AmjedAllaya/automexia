# Changed

- Make `Ctrl+V` paste through Automexia's existing safe clipboard path in
  every Windows-hosted terminal session, including WSL and SSH, while retaining
  `Ctrl+Shift+V` and allowing an explicit `ReceiveChar` override for terminal
  applications.
