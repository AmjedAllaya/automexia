Keep Linux release SBOM scratch private, pin the observed generator, and require
bounded privacy and semantic validation before signing. Normalize only approved
scan locations while preserving dependency, license and hashed-file evidence;
reject incomplete graphs and unsafe metadata. Published immutable assets remain
unchanged. Add CLI, bundle, mutation, resource-boundary and failed-write tests.
