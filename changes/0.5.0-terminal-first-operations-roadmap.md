Added

- Define the terminal-first remote-operations product strategy: one typed
  operation registry for the canonical CLI, leader-key command mode, fuzzy
  pickers, accessible overlays, and the complete v0.5+ connection, inventory,
  identity, action, workspace, file, log, collaboration, multi-cloud, policy,
  and AI roadmap.
