# Add bounded signed settings metadata decoding

- Decode versioned feature and typed option declarations from an optional signed package member through the existing verifier and pure ecosystem domain.
- Bind metadata to verified package identity, bound text and control counts, and return content-free typed errors for invalid optional metadata.
- Preserve manifest v1, legacy package acceptance, signing contracts and disabled execution. This source precursor does not activate installed settings or alter package-store recovery.
