# Development cache lifecycle

- Share pinned assurance tools through an integrity-checked,
  content-addressed cache while keeping mutable Cargo targets isolated.
- Add bounded cache inventory and dry-run-first cleanup with leases, process
  ownership, grace periods, and explicit scopes.
- Reclaim disposable verification and QA benchmark compilation artifacts and
  reduce normal debug-information storage without changing release profiles.
- Keep GitHub's free hosted CI automatic and make the local pre-push hook a
  documented, non-blocking placeholder until maintainers deliberately re-enable
  it.
- Repair source-owned Connection Hub documentation anchors and keep the
  packaging evidence ledger within its enforced ownership ceiling after the
  full mutation suite exposed the drift.
