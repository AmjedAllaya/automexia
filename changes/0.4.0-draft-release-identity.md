# Verify the actual GitHub draft lifecycle

- Resolve newly uploaded Linux drafts by numeric release ID, bound to the
  creation URL, instead of querying a tag that does not exist yet.
- Verify temporary draft and asset locators without allowing them in the final
  immutable, version-pinned release. Publish the exact verified numeric ID.
- Add real-draft regression, strict identity/URL, CLI no-write and workflow
  mutation coverage. Preserve signed bundle, governance and attestation gates.
- Record the observed failed draft, real least-privilege validation and remaining
  publication gate; distinguish Linux Early Access from stable platform trust.
