Changed

- Add bounded S1 assurance for resize queue coalescing and generation-aware
  atomic renderer snapshots, including stale-result rejection, last-known-good
  retry behavior, and explicit target limits.
- Add a repository-owned visual comparator with bounded image decoding,
  reviewed masks/tolerances, atomic difference heatmaps, and path-free JSON
  verdicts without adding product-build file I/O.
- Add the collecting S2 performance pipeline: classified Criterion plus native
  Windows memory evidence, exact same-runner composition, a reviewed 30-day
  baseline builder, exact expiring waivers, 90-day nightly retention, and a
  tagged-release gate that fails closed above 5% latency or 10% memory. The
  first controlled 30-day baseline remains an external release prerequisite.