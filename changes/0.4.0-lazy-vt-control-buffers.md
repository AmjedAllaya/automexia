Security

- Allocate synchronized-update storage only when used and release unusually
  large OSC, APC, and synchronized-update high-water allocations after a
  sequence terminates or is cancelled.
- Preserve small-buffer reuse, add overflow-safe size accounting, allocation
  regressions, architecture enforcement, and a parser-construction benchmark.
