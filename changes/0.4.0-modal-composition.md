Fixed

- Render the command palette and close confirmation in an explicit final modal
  composition phase on WGPU, Vulkan, Metal, and CPU backends, keeping all
  ordinary window, pane, footer, scrollbar, and text content visible only behind
  the modal scrim instead of clearing labels or allowing chrome to paint above it.
- Replace the tiny quit prompt with a responsive, opaque, high-contrast Automexia
  dialog, explicit Cancel/Close actions, keyboard hints, pointer hit testing, and
  exclusive input ownership. Add deterministic compositor ordering/layout tests
  and native WGPU/CPU state plus screenshot validation.
- Stop rectangle-only compositor batches from emitting redundant zero-vertex draw
  commands.
- Restore clean Linux X11-only, Wayland-only, and combined/all-feature builds by
  declaring the GLSL compiler in CI/release runners, aligning rio-window's
  calloop version with Smithay Client Toolkit, routing native diagnostics through
  rio-backend, updating CPAL 0.18 usage, and decoding the embedded icon through
  Automexia's bounded image policy.
