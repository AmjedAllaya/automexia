Changed

- Turn pane and all-visible-pane terminal search into one continuous session:
  the scope shortcuts now switch in place while preserving the query and focus,
  and repeating the active scope shortcut simply refocuses the query.
- Make `PANE` and `ALL PANES` clickable and keyboard-operable, add a bounded
  visible-match status, keep the surface responsive and clear of window
  controls, and publish privacy-safe renderer-neutral scope announcements.
