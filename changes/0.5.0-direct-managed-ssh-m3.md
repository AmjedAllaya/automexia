Changed

- Freeze the nonactivated direct managed-SSH request to 17 defensive OpenSSH
  options plus one typed destination, bound to a fresh complete review and
  revalidated native executable identity.
- Reconcile actual child exit into distinct success, failure, unavailable-status,
  and cancellation outcomes with fixed redacted desktop notifications.
- Add a nonblocking bounded connection-worker sink, private atomic 256-record/
  2-MiB completion receipt store with previous-generation recovery, and opaque
  current-source reconnect preparation that always requires fresh approval.
- Keep Connection Library and managed receipts independent from Quick Actions via
  a connection-owned no-follow, private-permission, native link/reparse adapter
  with stable Windows handle identity revalidation.
- Preserve immutable schema-1/schema-2 launch contracts and enforce schema 3,
  including OpenSSH post-quantum default and weak-crypto warning preservation.

Security

- Keep production managed launch compile-time disabled and the linked package
  unverified until protected exact-head approvals, real attestation/revocation,
  current controller observation, native forced descendant cleanup, OpenSSH,
  resource, and controlled accessibility gates pass.
- Exclude destination, terminal content, credentials, paths, environment values,
  process IDs, and executable identity from durable managed receipts and their
  debug/notification surfaces.
