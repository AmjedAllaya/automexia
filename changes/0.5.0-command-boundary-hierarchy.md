# Distinguish command boundaries from pane dividers

Replace pane-spanning command rules with short inset status accents. Preserve
timestamps, duration, output bands, pulse/reduced motion, copy behavior and pane
resize targets. Add malformed/responsive geometry, parser/navigation and exact
controlled pixel regressions; update the native acceptance contract and measure
the existing draw-only owner. Native desktop/accessibility review remains open.
