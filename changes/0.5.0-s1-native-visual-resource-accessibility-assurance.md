# S1 native, visual, resource, and accessibility assurance

- Added a production-absent `visual-test-hooks` fixture with fixed clock,
  animation, and public DevOps state for deterministic native captures.
- Split Windows Application Verifier into bounded Basics and low-resource
  phases with exact-target cleanup, redaction, log limits, and failure checks.
- Added a strict 24-suite evidence policy and mutation-tested validator for
  native, hardware/resource, visual, and assistive-technology proof.
- Added controlled S1 and stable-release gates that require fresh, complete,
  current-commit evidence and independent visual/accessibility review.
- Made native resource soak deterministic while preserving real pointer UX,
  live shell identity, route isolation, WGPU/CPU pixels, and cleanup checks.
