Command context and completion labels share a wrapping layout in narrow panes.
Extra information lines use display-only space, with matching cursor, selection,
scrolling and image coordinates. Native terminal rows and copied output remain
unchanged. Wider panes restore the single-row layout when the labels fit.
