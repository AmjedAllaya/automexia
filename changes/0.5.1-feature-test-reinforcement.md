Changed

- Add a machine-enforced reinforcement contract for all 39 registered feature
  families, with 117 required boundary/negative scenarios, cross-feature
  interactions, independent oracles, real evidence owners, native/platform exit
  criteria, and mutation tests that reject weakened coverage.
- Make deterministic visual comparison reject one changed channel in one pixel
  with no default masks and report the first changed coordinate, changed bounds,
  pixel/channel counts, and diff artifact.
- Expand each S1 visual suite from 560 to 1,600 required captures across 20
  named surfaces, and freeze native long-output, resource-lifecycle, scoped
  search, modal, accessibility, high-contrast, and reduced-motion scenarios.
- Record command-result grouping as partial until a real long-output viewport
  regression proves and fixes the known scrollback ownership gap.
