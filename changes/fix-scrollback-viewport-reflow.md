Keep retained scrollback content anchored during width and height changes using
the existing bounded grid reflow owner. Add parser-to-snapshot, selection,
alternate-screen and eviction regressions plus a scrolled deep-history benchmark.
Native redraw, pixel and accessibility validation remain separate requirements.
