---
kind: added
area: connectivity
---

- Add the bounded provider-neutral M7 authentication framework with immutable
  session capsules, 19 truthful lifecycle states, generation-safe refresh and
  rebind, last-known-good public status, and explicit revoke/disable/uninstall.
- Require exact visible one-time capability review for later official-CLI
  operations while keeping browser/device/MFA, credentials, certificates, and
  provider caches under the official CLI's ownership.
- Reject secret-bearing arguments, global provider-context mutation, stale or
  cross-session results, and passive provider-tool launch. Provider-specific
  adapters and real native login evidence remain independently gated.
- Parse bracketed IPv6 authorities strictly, allow only exact process/network
  capability scope, bind every visible operation/isolation/browser/risk field,
  preserve initial freshness, and atomically publish only fully validated
  capsule-pinned provider context.
