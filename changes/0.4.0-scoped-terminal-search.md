### Changed

- Replaced the top-right terminal search overlay with a branded pane-footer
  search for `Ctrl+F` / `Cmd+F`, and added a bottom-centered all-visible-pane
  search for `Ctrl+Shift+F` / `Cmd+Shift+F` that stays clear of window controls.
- Corrected initial active-route ownership so local search replaces the first
  pane's footer immediately instead of using its compact safe fallback.
