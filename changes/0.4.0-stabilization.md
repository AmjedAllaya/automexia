Fixed

- Isolate legacy Rio state during configuration migration and extension
  disable operations, supervise the extension worker lifecycle, harden built-in
  filter extraction, and execute OSC/Unicode/PTY conformance fixtures.
- Remove incompatible and obsolete vendored assets, preserve third-party
  notices, centralize packaging automation, and enforce native package
  install/uninstall and controlled GPU/WSL release checks.
- Integrate the supplied Automexia raster mark into Windows executable
  resources, native window icons, MSI/ZIP/macOS/Linux packaging, and a
  checksum-verified deterministic asset-generation workflow.
- Remove the unreachable frontend font-cache module and its vulnerable `lru`
  dependency, and align packaging automation with the pinned cargo-packager
  command interface.
- Add `cargo dev`, `cargo automexia`, and `cargo ready` as the cross-platform
  one-command development, launch, and contributor-readiness workflows.
- Replace repetitive duplicate-dependency warnings with an exact, reasoned
  compatibility baseline; new duplicate versions now fail dependency policy.
- Detach successful development launches so Automexia does not retain Cargo's
  build lock for the lifetime of the terminal window.
- Summarize successful workspace test harnesses in `cargo ready` while retaining
  complete compiler and test diagnostics whenever the command fails.
- Enforce LF text normalization and mark packaged, brand, and font assets as
  binary so Windows Git settings cannot create line-ending warning noise.
- Keep native PowerShell distinct from WSL even after nested-shell metadata,
  and preserve live DevOps prompt rows and their context through grid reflow.
- Wake the exact originating terminal route as soon as asynchronous DevOps/WSL
  discovery publishes its snapshot, so startup and shell-switch context appears
  without waiting for user input or another terminal action.
- Make new tabs and splits immediately reuse only fresh, fully equivalent
  session context while a live refresh runs; replace slow WSL Docker/Kubernetes
  CLI discovery with bounded configuration reads, and defer PowerShell icon/
  editor styling until after its first prompt is visible.
- Propagate metadata-only OSC 133 damage, assign stable per-prompt identities,
  and keep DevOps context visible across typing, command completion, scrollback,
  and terminal resize/reflow.
- Replace the grid's row-size-dependent unsafe swap with a complete safe row
  swap so new semantic metadata cannot corrupt rotation or graphics scrolling.
- Preserve byte-identical Bash/Zsh integration files during the Windows-to-WSL
  installer transfer instead of appending an extra trailing blank line.
- Add persistent two-tier native liquid-hacker chrome plus a renderer-owned
  operational-context row above every complete-path prompt, crisp bundled icon
  vocabulary, functional tab/actions/window controls, and an eight-direction
  resize frame for undecorated Windows windows.
- Track OSC 133 command start/end, exit status, and duration on stable prompt
  rows; retain completion metadata through resize/reflow and render durable
  right-aligned result badges plus semantic warning/error row backgrounds.
- Publish explicit PowerShell/Bash/Zsh identity, pair every context/path prompt
  row with a stable identity, keep complete paths free of duplicated badges,
  and distinguish Windows drive titles from POSIX/WSL paths.
- Add mockup-aligned `eza` file/folder icons to managed Bash and Zsh sessions,
  neutralize noisy DrvFs metadata colors, preserve user overrides and plain-ls
  fallback behavior, and apply Nerd Font icon metrics in the direct grid
  renderer so PUA glyphs are full-size and centered without affecting text or
  Powerline geometry.
- Discover Docker, Kubernetes, cloud, Terraform, Git and user context inside
  the active WSL distribution with bounded local-only probes, render truthful
  Docker/user badges, isolate snapshots by session, and refresh operational
  chrome every three seconds without waiting for another prompt.
- Increase default terminal readability to 20-point text with 1.20 line spacing,
  and render interactive eza long listings as labeled, color-separated metadata
  tables while preserving plain redirected output and user-defined palettes.
- Use a resize-safe three-row prompt contract across PowerShell, Bash, and Zsh:
  terminal-owned context/path rows, line-editor-owned editable input,
  atomic active-`aid` repaint replacement, coalesced PTY resizes, and a
  deterministic/native-GUI resize-stress gate. Preserve the shared prompt
  identity on every continuation, repair delayed shell-editor clear/repaint
  batches from a compact active-prompt snapshot, and restore Unicode paths from
  scrollback after extreme reflow without requiring user input.
- Remove speculative redraws after ordinary PTY keystrokes, add a no-damage
  terminal snapshot fast path, reuse row/context/metadata allocations, make
  Windows input-rate tracking constant-time at the queue front, and park its
  vsync worker completely while idle.
- Bound local and CI build storage with free-space preflights, disposable
  non-incremental verification targets, uncached CI build products, storage and
  purge commands, and generation-specific Windows runtime copies that leave
  Cargo's canonical executable unlocked.
- Display the extensions command-palette entry as plain `market` instead of the
  command-style `/market` label.
- Add bundled Nerd Font folder and file-type icons to native PowerShell `ls`
  output through a pipeline-safe four-column format view. Keep each glyph next
  to its filename in the final `Name` column while preserving `Get-ChildItem`
  objects, parameters, sorting, filtering, and redirection.
- Make chrome, grid margins, tabs, operational context, overlays and split
  geometry responsive from the 300×200 minimum through 8K/HiDPI viewports,
  with shared draw/hit-test breakpoints and Unicode-safe overflow handling.
- Add independent active-session cloning on `Ctrl+Alt+R` / `Ctrl+Alt+D`,
  preserving native and WSL shell/profile/directory intent while isolating PTY,
  process, input, scrollback, and extension state; cover it with deterministic,
  native ConPTY, resize-plus-clone, and controlled WSL regression gates.
- Give every exhaustive verification invocation a process/time-scoped build
  directory so concurrent or interrupted `cargo ready` runs cannot delete one
  another's compiler outputs.
- Keep Up Arrow and `Ctrl+R` history navigation responsive at deep scrollback
  by limiting prompt-protection work to the active live-edge block and locating
  completed commands backward from their newest output; cover native
  PowerShell history latency alongside the ConPTY resize storm.
- Optimize the VT parser/reflow hot path in contributor debug builds and let
  urgent input-driven redraw timers replace slower maintenance wakes.
- Honor ConPTY's private Win32 input-record mode, retain native virtual-key,
  scan-code, modifier, and extended-key metadata through the window backend,
  and deliver both key-down and key-up records without stealing bare Up Arrow,
  `Ctrl+R`, or `Ctrl+D` from the active shell.
- Close lost-wakeup races in the Windows PTY input and output ring buffers so
  the first key after an idle period cannot wait for a later key, resize, or
  maintenance event; cover repeated idle writes and real PowerShell history
  recall/search with native ConPTY latency regressions.
- Replace generic context palette buckets with semantic production, OS, Git,
  Kubernetes, Docker, provider-specific cloud, Terraform, environment, and
  user roles. Share exact brand anchors between live and historical context,
  keep the user lime green, and correct only lightness to maintain quantized
  WCAG AA contrast on customized themes.
- Reject relative/control-bearing live clone directories without discarding a
  safe stored profile directory, expand native PowerShell's adjacent-name icon
  vocabulary, and require controlled native GUI plus WSL resilience gates
  before stable release preflight.
- Make the one-command contributor gate validate native shell integrations,
  local Markdown links, and pinned Actions, and compile focused cache, worker,
  row-rebuild, and prompt-layout performance benchmarks nightly.
- Enforce coverage against modified and untracked local Rust sources, make its
  Git discovery wrapper-safe, and resolve release binaries from Cargo's
  effective target directory so isolated Windows MSI/ZIP packaging succeeds.
- Simplify the shell and local-time surface into one responsive glass rail with
  direct icon/value pairs and a restrained divider, removing nested cards,
  icon wells, and micro-labels while retaining shell-aware contrast.
- Normalize every operational-context glyph through fixed optical slots and
  per-icon metrics so Docker, Kubernetes, cloud, Terraform, environment, user,
  production, OS, Git, terminal, and clock marks share one visual baseline.
- Add one semantic path palette across PowerShell, Bash, and Zsh: muted
  separators, a light-blue root, blue parents, and a mint active directory,
  while preserving exact copied text and keeping prompt formatting
  subprocess-free.
- Keep route-isolated operational context visible in every split, including
  inactive panes, and draw a themed inset focus outline around the selected
  pane without changing its grid geometry.
- Replace the font-dependent palette chevron with a crisp four-tile command
  center control, and redesign the menu with Automexia glass surfaces,
  semantic command categories, icons, key badges, and responsive geometry.
- Keep resize reflow non-destructive: rebuild the terminal-owned prompt only
  after a real shell-editor erase, compact its archived rows without clearing
  completed output, and snap optional cursor animation across geometry changes.
- Separate window and tab scope on Windows and Linux: `Ctrl+T` creates an
  independent Automexia window with its initial tab, while `Ctrl+Shift+T`
  continues to add a global tab to the current window and `Ctrl+Shift+N`
  remains a compatible new-window alias.
