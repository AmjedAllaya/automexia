Fixed

- Return the Automexia identity from XTGETTCAP and enforce Automexia copy in
  native dialogs and runtime/configuration errors.
- Make bare `Ctrl+C` copy a non-empty terminal selection while preserving shell
  interrupt behavior without a selection. Add selection-aware secondary-click
  copy/paste, retain middle-click primary-selection paste, and keep left click
  free of paste side effects so focus, selection, links, previews, and pane
  activation remain safe.
- Bound raw OSC, APC/graphics, and XTGETTCAP retention; safely discard oversized
  streams, cancel CAN/SUB without dispatching partial payloads, rate-limit
  payload-free diagnostics, and add exact-boundary/recovery tests plus nightly
  fuzzing.
- Make runtime configuration reload last-known-good: reject malformed config,
  theme, requested fonts, and global-hotkey changes before live state mutation;
  transactionally replace hotkeys with tested compensating rollback.- Isolate legacy Rio state during configuration migration and extension
  disable operations, supervise the extension worker lifecycle, harden built-in
  filter extraction, and execute OSC/Unicode/PTY conformance fixtures.
- Remove incompatible and obsolete vendored assets, preserve third-party
  notices, centralize packaging automation, and enforce native package
  install/uninstall and controlled GPU/WSL release checks.
- Integrate the supplied Automexia raster mark into Windows executable
  resources, native window icons, MSI/ZIP/macOS/Linux packaging, and a
  checksum-verified deterministic asset-generation workflow.
- Remove the unreachable frontend font-cache module and its vulnerable `lru`
  dependency, and align packaging automation with the pinned cargo-packager
  command interface.
- Add `cargo dev`, `cargo automexia`, and `cargo ready` as the cross-platform
  one-command development, launch, and contributor-readiness workflows.
- Replace repetitive duplicate-dependency warnings with an exact, reasoned
  compatibility baseline; new duplicate versions now fail dependency policy.
- Detach successful development launches so Automexia does not retain Cargo's
  build lock for the lifetime of the terminal window.
- Summarize successful workspace test harnesses in `cargo ready` while retaining
  complete compiler and test diagnostics whenever the command fails.
- Enforce LF text normalization and mark packaged, brand, and font assets as
  binary so Windows Git settings cannot create line-ending warning noise.
- Keep native PowerShell distinct from WSL even after nested-shell metadata,
  and preserve live DevOps prompt rows and their context through grid reflow.
- Wake the exact originating terminal route as soon as asynchronous DevOps/WSL
  discovery publishes its snapshot, so startup and shell-switch context appears
  without waiting for user input or another terminal action.
- Make new tabs and splits immediately reuse only fresh, fully equivalent
  session context while a live refresh runs; replace slow WSL Docker/Kubernetes
  CLI discovery with bounded configuration reads, and defer PowerShell icon/
  editor styling until after its first prompt is visible.
- Propagate metadata-only OSC 133 damage, assign stable per-prompt identities,
  and keep DevOps context visible across typing, command completion, scrollback,
  and terminal resize/reflow.
- Replace the grid's row-size-dependent unsafe swap with a complete safe row
  swap so new semantic metadata cannot corrupt rotation or graphics scrolling.
- Preserve byte-identical Bash/Zsh integration files during the Windows-to-WSL
  installer transfer instead of appending an extra trailing blank line.
- Add persistent native liquid-hacker chrome with a pane-local tab rail only
  when the selected pane needs it, plus a renderer-owned operational-context
  row above every complete-path prompt, crisp bundled icon vocabulary,
  functional tab/window controls, and an eight-direction resize frame for
  undecorated Windows windows. Remove duplicated global session status, shell,
  clock labels, and the redundant workspace-action shelf so single-tab panes
  recover the unused terminal space.
- Make Ctrl+T tabs inherit the live window viewport before their first frame so
  a reduced PTY height cannot strand the footer in the middle of the window.
  Seed their visible profile from semantic/launch metadata before shell output,
  and replace GPU-diagonal close squares with one DPI-stable rasterized X.
- Add a responsive, read-only renderer-owned footer to every usable pane with a
  minimal `UTF-8`, session-aware `LF`/`CRLF`, grid-size, and live local-clock
  status line. Show pane/tab identity, history offset, and selection only when
  relevant. Reserve footer space outside the PTY and scrollbar, omit embedded
  action controls and hidden hit targets, collapse status labels on narrow
  panes, and return the full strip to terminals below the safe height threshold.
  Connect a single footer directly to both window edges; tile split footers at
  their exact pane seam, retain the pane root's vertical chrome origin, and
  extend the active-pane accent through its segment.
- Track OSC 133 command start/end, exit status, and duration on stable prompt
  rows; retain completion metadata through resize/reflow and render durable
  right-aligned result badges plus semantic warning/error row backgrounds.
- Publish explicit PowerShell/Bash/Zsh identity, pair every context/path prompt
  row with a stable identity, keep complete paths free of duplicated badges,
  and distinguish Windows drive titles from POSIX/WSL paths.
- Add mockup-aligned `eza` file/folder icons to managed Bash and Zsh sessions,
  neutralize noisy DrvFs metadata colors, preserve user overrides and plain-ls
  fallback behavior, and apply Nerd Font icon metrics in the direct grid
  renderer so PUA glyphs are full-size and centered without affecting text or
  Powerline geometry.
- Discover Docker, Kubernetes, cloud, Terraform, Git and user context inside
  the active WSL distribution with bounded local-only probes, render truthful
  Docker/user badges, isolate snapshots by session, and refresh operational
  chrome every three seconds without waiting for another prompt.
- Increase default terminal readability to 20-point text with 1.20 line spacing,
  and render interactive eza long listings as labeled, color-separated metadata
  tables while preserving plain redirected output and user-defined palettes.
- Use a resize-safe three-row prompt contract across PowerShell, Bash, and Zsh:
  terminal-owned context/path rows, line-editor-owned editable input,
  atomic active-`aid` repaint replacement, coalesced PTY resizes, and a
  deterministic/native-GUI resize-stress gate. Preserve the shared prompt
  identity on every continuation, repair delayed shell-editor clear/repaint
  batches from a compact active-prompt snapshot, and restore Unicode paths from
  scrollback after extreme reflow without requiring user input.
- Make active prompt recovery immutable per generation, propagate semantic
  ownership through scalar and optimized ASCII/Unicode writers, discard stale
  shell-editor repaint cells, close context ownership on a hard shell newline,
  and repair the final effective resize even when no later PTY input arrives.
- Report Windows PowerShell, PSReadLine, and PowerShell 7 health in
  `cargo xtask doctor`; warn non-destructively when the legacy 5.1/2.0 stack
  explains roughly one-second Up/reverse-history latency.
- Remove speculative redraws after ordinary PTY keystrokes, add a no-damage
  terminal snapshot fast path, reuse row/context/metadata allocations, make
  Windows input-rate tracking constant-time at the queue front, and park its
  vsync worker completely while idle.
- Bound local and CI build storage with free-space preflights, disposable
  non-incremental verification targets, uncached CI build products, storage and
  purge commands, and generation-specific Windows runtime copies that leave
  Cargo's canonical executable unlocked.
- Detect WSL source or Cargo targets under mounted Windows drives, report the
  effective workspace I/O mode in `cargo xtask doctor`, and stop
  compilation-heavy project workflows with a dual-native-checkout remedy.
  Windows-triggered image fuzzing now stages source, build output, and corpus
  state in disposable WSL-native storage.
- Display the extensions command-palette entry as plain `market` instead of the
  command-style `/market` label.
- Add bundled Nerd Font folder and file-type icons to native PowerShell `ls`
  output through a pipeline-safe four-column format view. Keep each glyph next
  to its filename in the final `Name` column while preserving `Get-ChildItem`
  objects, parameters, sorting, filtering, and redirection.
- Make chrome, grid margins, tabs, operational context, overlays and split
  geometry responsive from the 300×200 minimum through 8K/HiDPI viewports,
  with shared draw/hit-test breakpoints, one proportional scale for header
  controls/type/icons/local tabs, and Unicode-safe overflow handling.
- Add independent active-session cloning on `Ctrl+R` / `Ctrl+D`,
  preserving native and WSL shell/profile/directory intent while isolating PTY,
  process, input, scrollback, and extension state; cover it with deterministic,
  native ConPTY, resize-plus-clone, and controlled WSL regression gates.
- Give every exhaustive verification invocation a process/time-scoped build
  directory so concurrent or interrupted `cargo ready` runs cannot delete one
  another's compiler outputs.
- Keep Up Arrow and forwarded `Ctrl+R` history navigation responsive at deep scrollback
  by limiting prompt-protection work to the active live-edge block and locating
  completed commands backward from their newest output; cover native
  PowerShell history latency alongside the ConPTY resize storm.
- Optimize the VT parser/reflow hot path in contributor debug builds and let
  urgent input-driven redraw timers replace slower maintenance wakes.
- Honor ConPTY's private Win32 input-record mode, retain native virtual-key,
  scan-code, modifier, and extended-key metadata through the window backend,
  and deliver both key-down and key-up records. Keep bare Up Arrow shell-owned;
  use bare `Ctrl+R`/`Ctrl+D` for cloning and `Ctrl+Alt+R`/`Ctrl+Alt+D` for
  explicit history-search/EOF passthroughs.
- Close lost-wakeup races in the Windows PTY input and output ring buffers so
  the first key after an idle period cannot wait for a later key, resize, or
  maintenance event; cover repeated idle writes and real PowerShell history
  recall/search with native ConPTY latency regressions.
- Replace generic context palette buckets with semantic production, OS, Git,
  Kubernetes, Docker, provider-specific cloud, Terraform, environment, and
  user roles. Share exact brand anchors between live and historical context,
  keep the user lime green, and correct only lightness to maintain quantized
  WCAG AA contrast on customized themes.
- Reject relative/control-bearing live clone directories without discarding a
  safe stored profile directory, expand native PowerShell's adjacent-name icon
  vocabulary, and require controlled native GUI plus WSL resilience gates
  before stable release preflight.
- Make the one-command contributor gate validate native shell integrations,
  local Markdown links, and pinned Actions, and compile focused cache, worker,
  row-rebuild, and prompt-layout performance benchmarks nightly.
- Make the local contributor gate compile, lint, and test every workspace
  feature just like hosted CI, and lock the exact feature-sensitive command
  arguments with a regression contract.
- Keep a direct assurance-triggered product build on the contributor Cargo home
  while pinned scanners and audit tools retain their isolated Cargo home, so
  native dependency source roots cannot invalidate the shared build target.
- Bound the private GitHub Free Linux quality job to one compiler and test
  worker, omit disposable test debug information, and reclaim lint artifacts
  before the all-feature test build so the 8-GiB/14-GB runner stays responsive.
- Make Ghostty migration source/include/destination link rejection use one
  stable, role-specific `symbolic link` diagnostic, and retain exact real-Unix
  symlink assertions so hosted Linux cannot silently diverge from local Windows.
- Make the stand-alone Sugarloaf and rio-backend image/resource test graphs
  explicitly enable and forward both supported Linux display backends, and
  mutation-check each edge so a workspace-wide Cargo feature union cannot hide
  an invalid focused CI command.
- Enforce coverage against modified and untracked local Rust sources, make its
  Git discovery wrapper-safe, and resolve release binaries from Cargo's
  effective target directory so isolated Windows MSI/ZIP packaging succeeds.
- Simplify the shell and local-time surface into one responsive glass rail with
  direct icon/value pairs and a restrained divider, removing nested cards,
  icon wells, and micro-labels while retaining shell-aware contrast.
- Normalize every operational-context glyph through fixed optical slots and
  per-icon metrics so Docker, Kubernetes, cloud, Terraform, environment, user,
  production, OS, Git, terminal, and clock marks share one visual baseline.
- Add one semantic path palette across PowerShell, Bash, and Zsh: muted
  separators, a light-blue root, blue parents, and a mint active directory,
  while preserving exact copied text and keeping prompt formatting
  subprocess-free.
- Keep route-isolated operational context visible in every split, including
  inactive panes, and draw a themed inset focus outline around the selected
  pane without changing its grid geometry.
- Replace the font-dependent palette chevron with a crisp vector command-list
  control, remove the redundant menu header, and use a unified purpose-drawn
  outline icon grid, restrained semantic accents, key badges, and responsive
  geometry instead of icon-font glyphs and colored wells.
- Keep resize reflow non-destructive: rebuild the terminal-owned prompt only
  after a real shell-editor erase, compact its archived rows without clearing
  completed output, and snap optional cursor animation across geometry changes.
- Treat PSReadLine/Readline/ZLE carriage-return plus erase-line repaints as
  repair candidates: at tiny widths the editable row can share reflowed cells
  with the terminal-owned path. Repair remains a no-op when the semantic path
  block is complete and restores it automatically when usable space returns.
- Separate all three tab scopes: `Ctrl+T` adds a window-level tab,
  `Ctrl+Shift+T` adds an independent PTY tab inside the selected split/session,
  and `Ctrl+Shift+N` creates a separate OS window. Pane-local tabs preserve the
  selected launch descriptor, render in their own responsive rail, and close
  without tearing down sibling tabs or the window.
- Give the renderer-owned close control true OS-window scope. A secondary
  `Ctrl+Shift+N` window now closes without terminating sibling windows or the
  Automexia process. Centralize custom, keyboard, native, and final-PTY window
  teardown; cancel all route timers; invalidate settings/quake references; and
  reserve confirmation for the final unconfirmed window while keeping explicit
  `Quit` process-wide.

- Add a cross-shell, name-only filesystem taxonomy for sensitive,
  configuration, logs, source, documentation, tests, build output, media,
  packages, repositories, tools, data, cache, infrastructure, and packaging.
  PowerShell uses differentiated icons without changing native objects;
  PowerShell 7 and eza add safe category colors while legacy PowerShell keeps
  narrow table width intact.
- Replace stand-alone category symbols with true single-cell composite folder
  badges (lock, cog, text, source, information, check, sync, image, Git,
  wrench, table, clock, network, and archive). Add a TTY-only compatibility
  filter for Ubuntu/WSL eza 0.18.x so interactive Bash/Zsh listings receive the
  same badges and colors while redirected output remains byte-native.
- Increase complete-path separation with deterministic cyan/violet/blue parent
  roles and a lime active-directory anchor across PowerShell, Bash, and Zsh.
- Add an adaptive Windows PowerShell 5 category palette: normal direct VT
  windows use compact 8-bit color sequences, while narrow or redirected views
  automatically stay plain so the legacy table formatter cannot damage names.
- Add first-class Command Prompt integration: interactive `cmd`/`cmd.exe`
  remains inside the active Automexia ConPTY, publishes live shell/path
  metadata, receives the branded full-path prompt and category-aware `ls`/`ll`,
  clones as CMD, and leaves `cmd /c` plus built-in `dir` unchanged.
- Fix bare PowerShell `cmd` being normalized into one empty argument, which
  bypassed the interactive integration and opened a stock prompt. Reassert CMD
  identity on every repaint and PowerShell identity immediately after `exit`;
  deploy the batch as BOM-free ASCII, pass lambda through the Unicode launch
  command, force UTF-8 glyph output in the listing child, and cover the complete
  PowerShell-to-CMD-to-PowerShell flow in the native GUI/ConPTY stress gate.

- Add the Phase 0 assurance profile: pinned Nextest/JUnit/doctests, shrinking
  viewport/DPI properties with persisted regressions, reviewed structured
  footer snapshots, finite channel readiness Loom models, and a bounded
  redacted `cargo qa --bundle` report.
- Add Windows native handle/thread/private-bytes/working-set/descendant ceilings
  plus safe exact-target AppVerifier and WPR wrappers with guaranteed cleanup,
  private ETL handling, and controlled-runner nightly gates.
- Validate the final Windows client frame from the actual desktop compositor
  with bounded `ClientToScreen`/`BitBlt` capture while only the target is
  temporarily topmost; restore z-order in `finally`, enforce a strict
  five-second paint deadline, reject blank/single-color output, retain PNGs only
  when explicitly requested for private review, and exclude them from QA
  bundles. Gate custom-chrome close clicks on a painted secondary-window frame
  so an HWND-visible/application-not-ready race cannot create a false failure.
- Publish the v0.4 accessibility inventory/manual smoke matrix and accept the
  renderer-independent v0.5 semantic-model ADR without overstating current
  screen-reader support.
