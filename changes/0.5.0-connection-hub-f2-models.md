Added

- Added strict, bounded provider-neutral connection, profile, recipe, state,
  review, receipt, tunnel, and non-executing resolved-plan models.
- Added deterministic approval fingerprints, exhaustive authentication/result
  reducers, responsive accessible Connection Hub/review/planner projections,
  and synthetic all-provider/all-state fixtures.
- Fail closed on cross-recipe variable collisions and reject aggregate plan-step
  overflow before allocating or sequencing the resolved plan.- Added hostile/property/mutation tests, fuzz and 64-step benchmark ownership,
  architecture/CI ratchets, and an explicit all-disabled authority boundary.
- Recorded D5.0/F2 as locally complete but overall partially done while the
  protected ADR decision and all product/execution phases remain pending.
