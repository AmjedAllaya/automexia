CI repair: align the macOS Objective-C and Linux Wayland dependency families, run native shell integration checks on Windows, force libFuzzer to the GNU host target, install nFPM through its Go module, and provision benchmark system libraries.
