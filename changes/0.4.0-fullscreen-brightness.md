Fixed

- Keep Windows displays from idle-dimming while an Automexia window is
  fullscreen by owning and promptly releasing a per-window `DisplayRequired`
  request; preserve normal system sleep and user power-plan behavior.
- Keep Windows fullscreen rendering on an explicit SDR sRGB surface and verify
  borderless coverage, composited color stability, request lifetime, and
  restored window geometry in the native GUI stress gate.
