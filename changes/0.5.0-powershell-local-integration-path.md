---
kind: Fixed
component: shell-integration
---

Normalize validated local Windows shell-integration resources to a
PowerShell-compatible drive path, avoiding false RemoteSigned rejection of the
unsigned development script through Rust's verbatim canonical spelling. Strict
policies remain authoritative and now degrade quietly to the native shell
instead of printing a startup security exception.
