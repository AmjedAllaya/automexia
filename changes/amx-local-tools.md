Add explicit, bounded `amx find file`, `amx find text` and offline tealdeer-backed
`amx explain` commands. Preserve ripgrep ignores, escape untrusted output, preview
exact arguments, and keep WSL tool execution in the guest with cancellable process
ownership. Missing tools are reported and never installed automatically.
