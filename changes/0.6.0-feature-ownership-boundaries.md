Changed

- Separate provider-neutral connectivity and command-productivity contracts from
  the optional DevOps context package, keeping provider adapters independently
  disabled and making dependency ownership explicit.
- Make per-pane command-result spacing, exit color, and completion pulse core
  renderer behavior so it remains available when optional extensions are
  disabled.
- Add fail-closed architecture and mutation checks for package placement,
  provider/engine dependency direction, route isolation, and extension-independent
  command-result lifecycle.
