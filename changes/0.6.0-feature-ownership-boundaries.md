Architecture: clarified public feature ownership so core terminal mechanisms
remain in core/application owners, optional dependencies remain behind bounded
extension contracts, privileged I/O stays in the application broker, and
architecture checks reject reverse dependencies and duplicate state owners.
Private feature-family placement is not described publicly.
