Changed

- Connect the retained validated cached provider product from the Connection
  Hub to Quick Actions when the Action Center opens. Publication is limited to
  the exactly matching selected route, session, and capsule revision and never
  performs provider work while the user types.
- Make repeated publication idempotent and clear stale candidates on absence,
  mismatch, revocation, or route cleanup. Product failures expose stable,
  redacted, actionable notices and never send PTY input or execute a command.
- Extend the CP4 machine contract and integration coverage so repository policy
  now rejects removal of the real product handoff.
