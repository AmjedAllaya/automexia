Changed

- Enforce an acyclic Automexia-owned crate graph by moving Unicode-safe contract
  label normalization into `automexia-extension-api` and removing the
  `automexia-devops` dependency on `automexia-ui-model`.
- Add focused Unicode boundary tests, controlled microbenchmarks, and an ADR for
  the application composition root and lower-layer dependency rules.
