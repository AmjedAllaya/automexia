Added

- Added a clean-machine manual acceptance workbook covering every enforced
  feature-matrix entry with 175 detailed scenarios, expected results, negative
  and boundary cases, optional OpenSSH, Docker, Kubernetes, OpenShift, cloud and
  Teleport setup, native visual/accessibility/resource/package gates, and safe
  cleanup.
- Kept available, release-gated, internally nonactivated, preview-disabled,
  controlled-external, and planned-only behavior visibly separate so manual
  testing cannot turn missing external evidence into a false completion claim.
