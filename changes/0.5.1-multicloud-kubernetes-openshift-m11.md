---
kind: feature
area: connectivity
---

Add independently disabled Kubernetes and OpenShift source adapters with
bounded stable kubeconfig review, private cloud-output ingestion, fail-closed
merge and exec-plugin policy, capsule pinning, and exact nonactivated
`kubectl`/`oc` plans. Product execution and real client/cluster evidence remain
gated.
