Fixed

- Start `Shift`+Arrow selection at the terminal insertion cursor instead of an
  empty mouse-click anchor, and exit selection mode before forwarding a bare
  Arrow, typed text, clipboard paste, or IME input to the shell.
