Security

- Sealed validated connection profile and recipe wrappers, rejected hostile bidi
  plan overrides and duplicate review identities, correlated authentication
  completions with the active operation, canonicalized event IDs, and replaced
  planner panic paths with explicit errors.
- Preserved one managed Hub focus target after selection refreshes, added live
  loading progress and route-aware modal focus cycling, and replaced debug
  action summaries with stable labels that omit projected value contents.
- Expanded the F2 contract to 30 required regressions and added mutation ratchets
  for validation bypasses and planner panic primitives.
