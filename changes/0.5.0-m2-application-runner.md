Security

- Compile the exact session-launch broker in production behind a const-asserted
  false activation gate, and keep the linked first-party SSH package unverified
  so authorization stops before executable or filesystem resolution.
- Add one application-owned external-tool runner shared across windows with
  exact package/executable/argument/session/capsule/route binding, a 50-operation
  ceiling, 256-record redacted audit FIFO, bounded public environment allowlist,
  trusted working directory, cancellation, revocation, and shutdown cleanup.
- Hand the re-compared executable guard only to ContextManager, which creates
  one exact PTY, inserts one route, then marks the lease published; failed
  creation, scope, capacity, or publication cancels and revokes the operation.
- Add explicit Allow once, Allow for session, and Deny actions to Connection
  Review with pointer targets, A/Enter/S/D shortcuts, focus-safe behavior,
  accessibility semantics, icons, semantic color, and fixed redacted recovery
  messages.
- Keep managed SSH unavailable until ADR 0003 protected exact-head approvals,
  real package attestation/revocation, current executable observation in a fresh
  pre-launch review, native OpenSSH/descendant-cleanup/resource evidence, and
  controlled accessibility validation pass.
