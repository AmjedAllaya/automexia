### Added

- Search Google explicitly with `amx google <terms>` in integrated sessions,
  or `automexia google <terms>`. Includes offline URL preview, bounded encoded
  queries and collision-safe session helpers without global profile changes.
