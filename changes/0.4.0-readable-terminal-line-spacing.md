Changed

- Increase the shared default terminal line height from 1.15 to 1.22 for more
  readable command and output separation across Windows, WSL, Linux, macOS,
  PowerShell, CMD, Bash, and Zsh while preserving explicit user overrides.
