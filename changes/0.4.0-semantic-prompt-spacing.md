# Fixed

- Give renderer-owned prompt context tags a dynamic minimum 1.22-row vertical
  rhythm after preceding command output. The policy activates only when context
  content exists, uses each pane's actual row height, stays within the reserved
  semantic row, and keeps completion timing aligned across Windows, WSL, Linux,
  and macOS.
