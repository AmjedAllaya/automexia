Added

- Add a source-complete, nonactivated provider-aware Quick Actions boundary for
  cached SSH, AWS, Azure, Google Cloud, Kubernetes, OpenShift, and Teleport
  context. Rows show the exact public target, state, and environment risk with
  connection visuals and accessible text; current observations remain
  insert-without-Enter and production requires a second confirmation.
- Isolate immutable provider snapshots by route, session, capsule revision, and
  generation; reject stale results and revalidate immediately before copy or
  insertion. Broker-required or non-current contexts fail closed without using
  ambient provider state.
- Freeze CP4 limits and denied authorities with provider-owned exact grammar,
  core/application/UI regressions, mutation policy, hostile-capsule fuzzing, and
  cached snapshot/search benchmarks. Product provider publication/execution,
  OpenBao, and real provider/native release evidence remain gated.