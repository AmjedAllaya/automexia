### Changed

- Move internal research, exact unreleased feature designs, implementation
  ledgers, and unpublished drafts into an ignored local-only workspace.
- Remove public future-feature specifications, roadmaps, and summaries. Keep
  only documentation for the current free open-source terminal baseline and
  ordinary capabilities common in open-source terminals.
- Preserve the complete pre-privatization Markdown set in an ignored private
  snapshot so no planning information is lost.
- Where an unchanged source-owned validator still requires a retired path, keep
  only a non-activating compatibility notice with no feature or business detail.
- Extend repository validation so the retired public research pack cannot return,
  the private workspace remains ignored, and public Markdown cannot link into it.
- Add a publication policy that forbids credentials, personal machine details,
  local paths, and other private data in both public and local documentation.
