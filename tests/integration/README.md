# Integration suites

Native CI owns ConPTY and Unix PTY lifecycle, resize, child-exit, teardown, and
high-throughput coverage. Packaging runners own clean install, upgrade,
uninstall, desktop integration, URL handler, terminfo, and Rio-coexistence
checks. Controlled release runners own real-GPU and WSL smoke tests.
