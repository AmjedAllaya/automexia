
# Research / RFD Notes

These files intentionally avoid the project's ADR number namespace.

They are inputs to real project decisions, not accepted decisions themselves.

Known actual project owners from the supplied audit:
- proposed ADR 0025 — CP5 editor bridge;
- accepted ADR 0028 — bounded top-level tab parked-PTY undo/redo;
- proposed ADR 0029 — non-activating public ecosystem/sandbox/AI boundary.

When integrating an RFD, update the existing real owner or let the repository assign a new ADR number.
