
# Accessibility Deltas

**Type:** Research/Request-for-Discussion note — not a project ADR
**Repository integration note:** Accepted project ADR 0013 owns this direction; this page records only remaining evidence deltas.

This file preserves proposal reasoning without claiming an ADR number or acceptance status.

---


## Accepted direction and remaining delta

Automexia's renderer-independent semantic models are partially implemented.
Accepted ADR 0013 selects an AccessKit-backed platform adapter direction where
compatible with the frontend/window stack, with native augmentation for

Keyboard completeness remains mandatory. Pointer behavior is optional convenience. Color/icon state is supplemental and authoritative text/semantics must exist.

Release qualification still requires native screen-reader validation on supported platforms.
